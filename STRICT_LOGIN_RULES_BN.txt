Elite V19 Premium Secure Rules

Firestore Rules:
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /ashaMiniShallow/{docId} {
      allow read, write: if request.auth != null
        && request.auth.uid == "FZIhTAr0JEMoHCeQd3kr2t8cJKk2"
        && docId == "ekramul-main";
    }
  }
}


Login screen now shows only Email + Password.
Owner UID/details are hidden from user UI.
Only admin email should be used: sktubay786@gmail.com
